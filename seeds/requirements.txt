# Python dependencies cho đồ án Cơ Sở Dữ Liệu Phân Tán

# Seed data generation
Faker>=19.0.0

# Password hashing (khớp với /app/AuthService - bcrypt)
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# Database connection (khi cần)
psycopg2-binary>=2.9.9
asyncpg>=0.29.0

# Redis client (khi cần)
redis>=5.0.0

# Elasticsearch client (khi cần)
elasticsearch>=8.11.0

# FastAPI (khi cần)
fastapi>=0.109.0
uvicorn>=0.25.0

# HTTP client (requests)
requests>=2.31.0
